namespace BattleRoyale.Level
{
    /// <summary>
    /// An enum to represent the state of a level
    /// </summary>
    public enum LevelState
    {
        SPAWNING,
        LEVELFAILED,
        LEVELCOMPLETE
    }
}
