namespace BattleRoyale.Level
{
    /// <summary>
    /// An enum to determine the items type currently being spawned
    /// </summary>
    public enum SpawnType
    {
        WEAPON,
        CHARACTER
    }
}
